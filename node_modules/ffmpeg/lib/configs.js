/**
 * Basic configuration
 */
var path = require('path');
var appDir = path.dirname(require.main.filename);

module.exports = function () {
	this.encoding	= 'utf8';
	this.timeout	= 0;
	this.maxBuffer	= 200 * 1024;
	//模块目录为相对的根目录
	this.ffmpegPath = appDir + "/ffmpeg/bin/ffmpeg.exe";
}